import { MD3Theme } from 'react-native-paper';

export const styles = (theme: MD3Theme) => ({
  checkbox: {
    width: 40,
    height: 40,
    borderRadius: 6,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
